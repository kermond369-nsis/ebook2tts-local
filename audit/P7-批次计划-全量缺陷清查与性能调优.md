# P7 批次计划 — 全量缺陷清查与性能调优

> 版本 **v0.2（定稿候选）** ｜ 2026-09-16 ｜ 归属：`ebook2tts-local`（书声本地）P7 批次
> 依据：甲方 2026-09-16 指示「全方位检查各方面 bug，同时对其性能调优；另 fork 到其余仓库建新分支改动，审查通过并经甲方确认后方可合并主仓库」
> 纪律基线：`AGENTS.md`（文档权威 / 九条红线 / 禁止提交项 / 子代理纪律）；编号续排 **不复用不重排**（IM 从 **IM-523** 起、缺陷用 **BUG-P7-###**、决策用 **DQ-P7-#**）
> 复核：v0.1 已经 **agy 独立复核**（附件N），v0.2 已逐条处置（见 §十二）
> 状态：**甲方已于 2026-09-16 批复并开工 P7-A**（批复登记见 §十四）

---

## 一、目标与范围

### 1.1 目标（可验收的四件事）

| # | 目标 | 出口物 |
|---|---|---|
| G1 | **全量缺陷清查**：对三模块 + 构建/合规/安全面做穷举式清查，产出**可复现、有证据、已分级**的缺陷台账 | 《P7-B 缺陷台账》（BUG-P7-###，每条附复现步骤与原始证据） |
| G2 | **性能基线**：按《需求书》§5.1/§5.2 与 TC-01~15 的口径，产出可复算的实测基线（含采集脚本与原始数据） | 《P7-A 基线与环境报告》＋原始 trace/日志归档 |
| G3 | **缺陷修复**：按分级（P0→P1→P2）分批修复，每批文档回写 + 自测 + agy 独立复核 + 真机证据 | 各修复批次计划与复核报告（附件序列） |
| G4 | **性能调优**：对 G2 量化出的瓶颈逐项做同机同条件 A/B，达标或给出可复算的改进幅度与归因 | 《P7-D 调优报告》（每项含前后对照数据） |

### 1.2 In Scope

- `:core`（纯 Kotlin：文本管线 / 角色与音色模型 / 状态机核心 / 线程策略 / 在线设置与映射）
- `:engine`（TTS 服务 / SynthesisCoordinator / SherpaBackend / NativeGate / 下载与清单 / SafeExtractor / MMKV 迁移 / 预览与协议 Activities / OnlineRouter·Backend·RoleRefiner / PcmSpeedStretcher）
- `:app`（Flutter 六页 UI、原生桥、下载前台服务、预览控制桥）
- 构建与交付面（两套 Gradle 构建、CI、APK 体积、R8/keep、manifest 与权限、FGS 合规）
- 合规与安全面（密钥存储与日志、SAF 导入、导出组件、明文流量、依赖 CVE）

### 1.3 Out of Scope（本轮不做，除非甲方另行裁决）

- 无 RQ 依据的**新功能**（导出音频、voiceclone、更多在线音色等）；
- **P5-B（AGP built-in Kotlin）**：仍受《P5-批次计划》§二.2.2 两道前置门约束，未过门不动，且**不得触碰 JNI 契约**；
- 修改 `SherpaBackend$generateStreamingLocked$1.invoke:([F)Ljava/lang/Integer;` 这一 JNI 契约；
- 直接向主仓库 `main` 推送任何改动（合并唯一入口＝PR，见 §三）。

---

## 二、方法总则（成熟项目优先，禁自造轮子）

**铁律落地**：清查与调优一律使用成熟、社区验证过的工具与官方 API；**禁止**自写"等效"检测/基准框架；**禁止**独立自证闭环——由 **agy 逐批次独立复核**（只读、可驳回）。

| 层面 | 工具（执行时以最新稳定版为准并记录版本） | 落地位置 |
|---|---|---|
| Kotlin 静态分析 | **detekt**（社区标准）+ **Android Lint**（AGP 9.0.1 内置，`:engine:lintDebug`） | 审计仓 CI job |
| 代码格式 | **ktlint**（可选，仅报告不阻断） | 审计仓 CI job |
| Dart 静态分析 | `flutter analyze` + `dart analyze --fatal-infos` + `dart format --set-exit-if-changed` | 审计仓 CI job |
| 依赖漏洞 | **OWASP dependency-check**（Gradle 插件）+ `flutter pub outdated` | 审计仓 CI job |
| 覆盖率 | **JaCoCo**（:core/:engine）+ `flutter test --coverage` | 审计仓 CI job |
| 边界/属性测试 | **kotest-property**（成熟 Kotlin property-based 测试）补文本管线/清单解析/Range 解析不变量 | `:core`/`:engine` 单测 |
| 微基准（纯 JVM） | **JMH**（OpenJDK 官方基准框架）测分句/角色分配/变速换算吞吐 | 独立 benchmark 源集 |
| 内存泄漏 | **LeakCanary**（`debugImplementation`，仅审计仓 debug 包）；**多进程支持须实测确认**——`:tts_service` 独立进程是否被抓取以官方文档 + 实测为准，不生效则降级为 `am dumpheap` 手动 hprof + Perfetto heap profile | 真机/模拟器 |
| 主线程纪律 | **StrictMode**（Android 官方）全量策略 + `dumpsys dropbox` | 真机/模拟器 |
| 性能 trace | **Perfetto**（官方）＋ **simpleperf**（官方）＋ `dumpsys gfxinfo framestats` | 真机/模拟器 |
| 启动耗时 | `am start -W`（官方）＋ 引擎徽标打点 | 真机/模拟器 |
| 基准框架 | **AndroidX Macrobenchmark**（官方）做冷/热启动与滚动相对对照 | 模拟器（仅相对值） |
| 音频客观量 | 既有口径：自相关 F0 中位数/分布 + 波形不连续检测（numpy/scipy 分析，**复用 P6 既有脚本口径**，不新增框架） | 离线分析 |
| **native 崩溃与 native 内存** | 本仓**无自研 native 源码**（实查：无 `*.cpp`/`CMakeLists.txt`），SherpaONNX 为**预编译 AAR**（`engine/libs/sherpa-onnx-1.13.8.aar`）⇒ **ASan/HWASan 无法插桩**（不可插桩第三方 .so）。替代（均为官方成熟手段）：`dumpsys dropbox` tombstone 抓取 + NDK **`ndk-stack`** 符号化 + Perfetto **native heap profile** + `dumpsys meminfo` Native Heap 曲线 | 真机/模拟器 |
| 网络性能 | OkHttp 打点 + Perfetto network trace；下载测速**复用既有 probe 打点** | 模拟器/真机 |

### 2.1 工具链兼容性（**已实查，2026-09-16**；接入前须在审计仓实测，不兼容即走降级路径）

本项目组合为 **Gradle 9.1.0 + AGP 9.0.1 + KGP 2.3.20 + JDK 17**，属较新组合，第三方插件支持情况须先验证再落地：

| 工具 | 实查结论 | 降级路径 |
|---|---|---|
| detekt | 稳定线 **1.23.8 官方兼容表仅到 Gradle 8.12.1 / AGP 8.8.1**；**2.0.0 仍为 alpha**（最新 alpha.6 构建于 Gradle 9.6.1 / AGP 9.3.1 / JDK 25）⇒ 与本项目组合**无官方兼容承诺**。**实测（2026-09-16，CLI 旁路）**：1.23.8 与 **2.0.0-alpha.6 均可在 JDK 17 下运行并完成分析**（后者无需 JDK 25）；但两版**默认规则集不同**（`:core` 分别报 92 / 60 项，**不可直接比较**），且 **2.x 报告参数名变更**（`md`→`markdown`、`xml`→`checkstyle`），沿用 1.x 写法会**静默不产出报告** | 用 **detekt-cli**（独立二进制/独立任务）旁路运行，不绑定 Gradle 插件；**本轮采用 1.23.8**（规则集与报告参数已验证可用），2.x 待稳定后再评估 |
| ktlint | 版本与 Gradle 9 兼容性未核实 | 同上，CLI 化 |
| OWASP dependency-check | Gradle 插件最新 **13.0.0（2026-08-03）**，未见对 Gradle 9 的官方兼容声明；且首次需下载 NVD 库（耗时长、有速率限制） | 独立 CLI/Gradle 独立任务，缓存 NVD 数据；必要时以 `flutter pub outdated` + 逐依赖人工核查 CVE 兜底 |
| JMH | Gradle 插件 `me.champeau.jmh` 最新 **0.7.3（2025-01）**，Gradle 9 兼容性未验证 | 独立 JVM 模块直接依赖 `jmh-core` + annotation processor，或用 CLI 跑 |
| JaCoCo | Gradle 内置（`jacoco` 插件），随 Gradle 9.1.0 走 | — |
| Android Lint | AGP 9.0.1 内置 | — |
| LeakCanary / Macrobenchmark / Perfetto / simpleperf / StrictMode / ndk-stack | Android 官方或 Square 既有成熟件；具体版本与 `compileSdk 36 / minSdk 27` 的兼容性接入时实测记录 | 见 §二 LeakCanary 行降级 |

> 说明：上表"未核实/未验证"字样表示**尚未实测**，不得在报告中表述为可用；P7-A 第一动作即逐项实测并回填结论。

---

## 三、仓库与分支流程（**待甲方裁决 DQ-P7-1**）

### 3.1 事实（已核实，2026-09-16）

- 主仓 `kermond369-nsis/ebook2tts-local`（public，owner = 当前账号本人）；
- **GitHub 不允许把仓库 fork 到拥有它的同一账号**，故**无法**用"Fork 按钮"得到同账号的 fork；该账号名下**没有** Organization。

### 3.2 方案选择（**2026-09-16 甲方二次裁定后定案**）

甲方口径澄清：「两个名字不一致的仓库——**一个是公开、在测试的是私密**；测试完成后把改动合并到主仓库」，选择权交实施线。

**最终选择＝方案 A'（新建 private 平行仓）**，与既有方案 A 的差别仅在**私密**与**命名**：

| 项 | 定案值 |
|---|---|
| 公开主仓（发布用） | `kermond369-nsis/ebook2tts-local`（**public**，工作期间 `main` 零改动、零推送） |
| 私密测试仓（工作用） | **`kermond369-nsis/ebook2tts-local-p7`（private，新建）** —— 与主仓**名字不一致**、内容含全部改动与 `audit/` 文档目录 |
| 工作分支 | `p7/audit`（本地与私密仓同名） |
| 合并闸门 | 私密仓全绿 + agy 复核 + 甲方确认 → 同一批提交推送至**主仓**分支 `p7/landing` → 主仓开 PR → 甲方确认合并 → 主仓 `main` 更新、CI/Release 复绿核验 |
| 为何不用 GitHub fork | GitHub **不允许** fork 到拥有它的同一账号（已核实），该账号亦无 Organization；`s/公开主仓/私密测试仓` 的平行仓方案等价且零额外账号动作 |

> 依据：甲方 2026-09-16 二次澄清（"两个名字不一致的仓库…一个是公开，在测试的是私密…合并到主仓库"）。

### 3.3 合并闸门（三方案通用，唯一入口）

```
审计仓 提交（每次一个批次） 
  → ① 全绿：:core:test / :engine:testDebugUnitTest / flutter analyze+test / 审计仓 CI（含静态门）
  → ② agy 独立复核报告（含九条红线逐条核验表），无"必须改"未闭环项
  → ③ 甲方确认
  → ④ 推送同一批提交至主仓分支 p7/landing（git fetch 审计仓 + push 主仓）
  → ⑤ **改动白名单审查**：PR diff 仅允许 `core/` `engine/` `app/` 源码与测试、`docs`（非公开约束类不入库）与必要文档；**禁止**携带审计仓专属 CI（`.github/workflows` 审计 job）、detekt/lint 配置、任何 secrets、代理配置、依赖清单变更（如确需改依赖须单列理由并经甲方逐条批准）
  → ⑥ 主仓开 PR（main ← p7/landing），甲方点合并（或甲方授权实施线合并）
  → ⑦ 合并后主仓 CI/Release 复绿核验，短 SHA 回填台账
```

**红线**：未过 ①②③ 一律不得出现在主仓任何分支上；审计仓的 CI 密钥/代理配置不得写入主仓。

---

## 四、缺陷清查矩阵（"全方位"的骨架，逐格可勾）

标记：**S**＝静态分析 / **D**＝动态实测 / **M**＝人工走查 / **T**＝测试补强

| 面 | 子项 | 方法 | 判据来源 |
|---|---|---|---|
| **core 文本管线** | 分句与首段微切、标点/无标点硬切、数字与章节标题、"第3节车厢"负例、空白/emoji/日英混排/超长单段 | S T M | RQ-108、TC-05、AR-§4.6 |
| **core 角色与音色** | 说话人抽取与清洗、角色档案命中、音色表与中文命名、未知音色回落 | S T | RQ-105/109、ADR-013 |
| **core 状态机/线程** | 状态迁移、线程自适应 `clamp(核数-1,2,8)`、空闲与并发边界 | S T | RQ-102/104、IM-522 |
| **core 在线设置与映射** | 密钥校验语义、性别/语言映射、开关持久化 | S T | RQ-501~509、IM-517/519 |
| **engine 服务生命周期** | `onCreate/onUnbind/onDestroy` 有界等待、反复重建（200 次）、被杀重启、FGS 类型与通知 | D M | AR-§4.8/§8.4、风险 7 |
| **engine 合成链路** | `SynthesisCoordinator` 九条红线逐条核验、流式分段、微淡化边界、`done()/error()` 契约、绝不假成功 | S D M | AR-§4.3/§4.8.6、九条红线 |
| **engine 并发与互斥** | `NativeGate` 唯一互斥点、试听与系统朗读并发、无死锁/无强停 | S D | IM-514、RQ-404 |
| **engine 下载与清单** | 多源测速质量、断点续传、SHA-256、空间预检 2.5×、取消即时生效、失败恢复、清单兜底与缓存 | S D T | RQ-201/202/206、IM-520/521 |
| **engine 解压与导入** | Zip Slip / 体积炸弹 / 清单伪造 / 原子落盘 / SAF 强制度 | D T | AR-§6.5、TC-11 |
| **engine 迁移与配置** | SP→MMKV 幂等、`.completed` 回填校验、双进程并发迁移 | S D T | RQ-205、AR-§11.1 |
| **engine 在线链路** | Router 择路、失败静默回落本地、密钥与日志纪律、超时与重试、多角色精标、变速不变调（采样率恒定） | S D | RQ-501/506~509、ADR-011/013/014 |
| **engine 预览与协议** | 试听语义分离、音频焦点、协议 Activities 导出面与权限 | D M | RQ-404、IM-518 |
| **app(Flutter)** | 六页状态与导航、平台通道错误面、生命周期与 dispose、长列表与内存、失败提示人话、无工程黑话 | S D M | RQ-3xx、AGENTS.md §5 |
| **app 原生壳** | MainActivity/桥接、下载前台服务、预览桥、进程零推理 | S M | AR-§1.7 |
| **九条红线专项（红线 1~9 逐条走查）** | 红线5：native 释放**仅在守卫锁内、无任何持有者时**；红线6：微淡化**仅限首块头部/末块尾部**，严禁下沉进流式分段路径；红线7：合成线程**严禁 IO / 去抖 / 锁长等待**（配置与映射预构建）；红线9：SAF 导入**强制经 SafeExtractor**，严禁另写解压循环；红线1/2/3/4/8 分别见「合成链路」「native 与进程」「迁移与配置」各行 | S D M | AGENTS.md §4、AR-§4.3/§4.8 |
| **异常注入与弱网** | 缺模型 / 坏包 / 存储满 / 进程被杀 / 断网 / 飞行模式 / **DNS 黑洞 / 极高延迟 / 限速丢包** → 在线**静默回落本地且不无声**、提示人话、无崩溃 | D | NFR-§5.2、TC-11/13 |
| **OEM 后台策略** | 双进程 `:tts_service` 与下载 FGS 在厂商 ROM（MIUI/OriginOS 等）下的**冻结/查杀/后台拉起限制**——**模拟器无法覆盖**，须真机验证 | D | AR-§12 风险1、AR-§8.4 |
| **构建与交付** | 两套 Gradle 构建边界、R8/keep 与 sherpa AAR、manifest 权限总表、targetSdk 34 合规、APK 体积与 ABI | S D | RQ-201、AR-§8.3/§9、NFR-§5.1 |
| **安全与隐私** | 密钥存储方式与回显、日志正文泄漏、明文流量、导出组件、依赖 CVE、许可清单 | S M | NFR-§5.4、AR-§8.3 |
| **稳定性压测** | 2 次/秒热更配置并持续合成 10min（PSS 增长 ≤10%、0 崩溃）、锁屏 2h、异常注入（缺模型/坏包/断网/被杀） | D | NFR-§5.2、TC-07/10/11 |
| **集成与兼容** | Legado 真机全流程（首接/多角色/换书）、系统设置链路（CHECK_TTS_DATA/样例朗读）、零模型协议、升级迁移 | D M | TC-01/04/12/13/14 |
| **连续性** | 句间空隙 P50/P95、丢句率、GAP 打点、爆音听测（耳机 100 句） | D | AR-§4.8.5、TC-06 |

**穷举要求**：每个格子必须落到「已查·无问题（附证据）」/「已查·有问题（BUG-P7-###）」/「本轮无法查（附原因与建议）」，**不允许留空**。

---

## 五、缺陷台账规范

| 字段 | 要求 |
|---|---|
| 编号 | `BUG-P7-###`（续排不复用） |
| 等级 | **P0** 崩溃/数据损坏/红线违反/朗读中断；**P1** 用户可感功能错误；**P2** 体验与一致性问题；**P3** 代码质量与可维护性（含静态分析命中） |
| 必填 | 标题 / 等级 / 模块 / 复现步骤（命令级）/ 现象 / 证据（日志原文或截图路径）/ 根因（代码位置）/ 影响面 / 是否涉红线（编号）/ 建议处置 / 状态 |
| 状态机 | 新建 → 已复核（agy）→ 修复中 → 已修（附证据）→ 已复核关闭 / 挂起（附理由） |
| 证据归属 | 原始日志与 trace 存审计仓 `evidence/p7/<BUG-ID>/`（不入主仓）；摘要与结论入《缺陷台账》交付 SMB |

---

## 六、性能协议（先量后治，禁主观描述）

### 6.1 环境与限制（**须甲方知晓，DQ-P7-2**）

- 现有调试目标：**MuMu 模拟器 x86_64 / Android 15**（`192.168.1.189:7555`），**无 ARM 真机**；
- 依 AR-§1.6「模拟器只做粗筛」，故：
  - 模拟器数据**只用于相对 A/B 与趋势**（同机同条件、前后对照）；
  - 《需求书》§5.1 的**绝对预算**（TTFT ≤500ms、RTF ≤0.5、PSS ≤220MB、双 ABI 包体 ≤60MB、功耗 ≤8%/h）**本轮无法在模拟器上完成判定**，需甲方提供 ARM 真机，或甲方裁决接受"基线 + 相对改进"口径；
  - x86_64 无法覆盖 `arm64-v8a/armeabi-v7a` 原生库性能特性（含 NEON 路径），结论须显式标注适用边界。

### 6.2 指标与采集（复用既有打点，不新增私有框架）

| 指标 | 采集方式 | 样本规则 |
|---|---|---|
| 引擎冷启/就绪 | `am start -W`（TotalTime/WaitTime）＋ 徽标 `INIT→CREATED→READY` 打点 | 冷启 ×5 取中位数，报 P50/P95 |
| 首包 TTFT（本地/在线） | 既有 `PERF|ttfb_ms` 打点 | 同文本 ×10，报中位数与分布 |
| RTF / total_ms / drop | 既有 `PERF|req=…` 打点 | 同文本 ×10 |
| 内存 | `dumpsys meminfo` PSS（总/Java/**Native**）＋ **Peak PSS（多次采样取 max）** ＋ Perfetto heap profile | 基线点 + 长跑每 10min |
| **功耗** | `dumpsys batterystats`（复位后按 NFR §5.1 标准条件：灭屏、飞行模式、耳机 50% 音量、1h 连续朗读）＋ `dumpsys battery` 电量曲线 | 每次 ×1，记录起止电量与时长 |
| **环境量（防误判）** | SoC/电池温度与热状态（`dumpsys thermalservice`、`/sys/class/thermal/*`）、热降频标记 | 每次测量随记，用于剔除热降频导致的漂移 |
| 卡顿/帧 | `dumpsys gfxinfo framestats` ＋ Perfetto | 滚动/翻页场景 ×3 |
| 下载吞吐与选源质量 | 既有 `probe|…`/`picked|…`/`GET|code=206` 打点 | 每源 ×3，记录时间与出口 |
| 包体 | `apkanalyzer`/`ls -l` APK + 各 ABI 拆分体积 | 每次构建 |
| 崩溃/ANR | `logcat`（`FATAL|SIGABRT|ANR in`）＋ `dumpsys dropbox` | 全批次全程 |

**采集纪律**：每次测量记录（提交 SHA / 设备 / ABI / 模型 / 文本 / 网络出口 / 时间），原始输出落盘，**禁止**只留结论数字；异常值弃用需给规则（如首轮预热不计入）。

### 6.3 已知调优候选（来自 P6 实测，非推断）

| # | 候选 | 现状证据 |
|---|---|---|
| T1 | **首包 TTFT**（本地 0.6~4s） | `total_ms ≈ 音频时长 + ttfb` ⇒ 瓶颈在首包与实时推流节拍，非推理吞吐 |
| T2 | **镜像测速质量** | 64KB Range 探测严重低估（探得 ~20KB/s，实下 ~2MB/s 级）⇒ 选源可能次优 |
| T3 | **在线链路延迟** | 在线首包受网络出口与模型侧影响；需分段归因（DNS/TLS/上送/首块返回） |
| T4 | **线程与模型档位** | 线程自适应已落地（模拟器 RTF 1.60→1.37~1.42）；档位/量化与核数组合未系统扫描 |
| T5 | **内存与长稳** | 2h 长稳与 200 次重建的内存曲线未测 |
| T6 | `Stop FGS timeout` | 取消瞬间系统日志（用户无感），观察项，顺手治 |
| T7 | 包体与启动 | 双 ABI/剥离符号/R8 对体积与冷启的影响未测（与 release 包相关，DQ-P7-4） |

---

## 七、批次划分与出口判据

| 批次 | 内容 | 出口判据 |
|---|---|---|
| **P7-A** 工具链与基线 | 审计仓建立、静态分析 CI 落地、Perfetto/StrictMode/LeakCanary 采集脚本与协议固化、全量基线采集（§6.2） | 静态分析首轮报告（问题全量入台账）+ 基线报告（含原始数据）+ 采集可复现（命令级） |
| **P7-B** 全量缺陷清查 | 按 §四 矩阵逐格执行（S/D/M/T），产出缺陷台账 v1.0 | 矩阵无空格；每条缺陷可复现、有证据、已分级；agy 复核通过 |
| **P7-C** 缺陷修复 | 按 P0→P1→P2 分批；每批：**文档回写 → 实施 → 自测 → agy 复核 → 证据归档** | 每批全绿 + agy 复核无未闭环"必须改"；回归无新增缺陷 |
| **P7-D** 性能调优 | 对 T1~T7 逐项定位→改→同机 A/B→结论（未达标须给归因与下一步） | 每项含前后对照原始数据与归因；涉行为变更者已回写文档 |
| **P7-E** 汇总与合并准备 | 三件套回写、台账关账、PR 到主仓、CI/Release 复绿 | PR 全绿 + 甲方确认合并 |

> 批次内部不设日历承诺（工作量与依赖驱动）；甲方若需排期，另出《P7 排期表》。

---

## 八、纪律与约束（本批次强制）

1. **改行为先改文档**：任何行为变更先落《需求书》/《架构书》/《实现报告》并升版（编号续排，不复用不重排），实现与文档不一致＝缺陷；
2. **九条红线**：每批次 agy 复核报告必须含「9 条防呆红线逐条核验表」；修复不得为指标牺牲红线（尤其"已有音频绝不强停""绝不假成功"）；
3. **禁止提交**：密钥/凭据、内网地址与主机名、本机绝对路径、构建产物、模型文件；
4. **密钥纪律**：只从投放文件读取，永不进对话、永不回显、永不入库/入日志；
5. **两套 Gradle 构建勿混**；APK 一律由 Flutter 工具产出；
6. **验证类进程用完即杀**（headless/Perfetto/simpleperf/CI runner）；
7. **子代理纪律（AGENTS.md §6）**：禁建/改技能、禁越界写盘、禁自行扩权（不得新增依赖、改 CI、push/建 PR），任务书须写明本条与「交付目录只放交付物」；
8. **交付纪律**：SMB `/home/hermes/inbox/output/ebook2tts-local/p7/`，属主 `hermes:hermes`、权限 660/770，IP 动态取，交付后 `systemctl restart smbd`；
9. **文档不经 GitHub**（AGENTS.md 与 `docs/*` 约束类文档不入库；两份用户可见法律文本除外）。

---

## 九、交付物与归档

| 交付物 | 路径（SMB `/home/hermes/inbox/output/ebook2tts-local/`） |
|---|---|
| 本计划（定稿） | `refactor-docs/P7-批次计划-全量缺陷清查与性能调优.md` |
| 基线与环境报告 | `p7/P7-A-基线与环境报告.md` + `p7/证据/` |
| 缺陷台账 | `p7/P7-B-缺陷台账.md`（v1.0 起逐批更新） |
| 修复批次计划/报告 | `p7/P7-C-修复批次-x.md` |
| 调优报告 | `p7/P7-D-调优报告.md` |
| agy 复核报告 | `refactor-docs/附件N-agy复核报告-P7-x.md`（连续编号） |
| 三件套回写 | `refactor-docs/需求书.md` / `架构书.md` / `实现报告.md`（升版） |

---

## 十、风险与限制（须如实登记）

| # | 风险/限制 | 应对 |
|---|---|---|
| R1 | **无 ARM 真机** ⇒ NFR 绝对指标无法判定 | DQ-P7-2 由甲方裁决；结论显式标注适用边界 |
| R2 | 在线链路结论受 MiMo 免费额度/限免政策与出口网络影响 | 每次测量记录时间与出口；政策变化时结论作废重测 |
| R3 | 静态分析首轮噪声大（历史代码） | 先全量报告入台账分级，**不**一次性大规模重构；P3 级择机处理 |
| R4 | 修复引入回归 | 每批全绿 + 回归清单 + agy 复核；红线段改动须专项复核 |
| R5 | release 正式包涉签名密钥与 R8 | 未获甲方授权（DQ-P7-4）不做；不代持任何签名密钥 |
| R6 | 模拟器数据被误当"达标"结论 | 报告固定标注「模拟器相对值」前缀，禁止推为绝对合规 |
| R7 | Perfetto/simpleperf 长时间抓取影响被测性能 | 采集协议固定：指标测量与 trace 采集分轮进行，不叠加 |
| R8 | **OEM ROM 冻结/查杀策略**（双进程 `:tts_service`、下载 FGS）在模拟器上无代表性 | 列为真机验证项（DQ-P7-2）；模拟器结论显式标注不覆盖该面 |
| R9 | **native 层不可插桩**（SherpaONNX 为预编译 AAR）：ASan/HWASan 无法用于第三方 .so | 改用 tombstone + `ndk-stack` + native heap profile（§二）；如需 ASan 需上游提供可插桩构建，本轮不做 |
| R10 | 静态分析工具与本项目工具链（Gradle 9.1/AGP 9.0.1/JDK 17）**无官方兼容承诺** | §2.1 已实查并给出 CLI 降级路径；P7-A 逐一实测回填 |

---

## 十一、待甲方裁决（DQ-P7）

| 编号 | 事项 | 实施线建议 |
|---|---|---|
| **DQ-P7-1** | 审计仓库形态：A 新建平行仓 `ebook2tts-local-p7`（推荐）/ B 建 org 走真 fork / C 同仓分支 | **A**（立即可行，主仓 main 零污染） |
| **DQ-P7-2** | 性能口径：接受"模拟器相对 A/B + 基线" / 提供 ARM 真机做 NFR 绝对值 | 优先争取真机；无则如实标注限制 |
| **DQ-P7-3** | 是否允许引入审计用工具依赖（detekt/ktlint/JaCoCo/JMH/kotest-property/OWASP dependency-check/LeakCanary/Perfetto 客户端）——**仅审计仓与本地**，其中 LeakCanary 仅 `debugImplementation`，主仓运行时依赖零新增 | 建议允许 |
| **DQ-P7-4** | release 正式包（剥离符号/双 ABI/正式签名/R8 验证）是否纳入本轮；keystore 由甲方提供且不由实施线代持 | 建议**并列 P7-D**，待甲方供密钥后启动 |
| **DQ-P7-5** | 修复范围红线：仅"最小改动 + 文档同步" / 允许为修 bug 做结构性重构（走文档升版） | 建议前者的默认口径，逐案请示 |
| **DQ-P7-6** | 优先级：先"用户可感缺陷"再性能 / 两者并行 | 建议：A/B 先行取证，C 按分级推进，D 并行做基线 |

---

## 十二、agy 独立复核（v0.1）与逐条处置

> 复核方：agy（只读、可驳回）；复核报告原文见 `附件N-agy复核报告-P7计划-v0.1.md`；结论：**有条件通过**。

| 复核项 | 意见摘要 | 处置 | 说明 |
|---|---|---|---|
| **MUST-1** | LeakCanary 在 `:tts_service` 多进程下可能完全不生效，造成"虚假安全感" | ✅ 采纳 | §二 LeakCanary 行加「多进程支持须实测确认」，并给出 `am dumpheap` + Perfetto heap profile 降级路径（是否真生效以官方文档 + 实测定，不作断言） |
| **MUST-2** | §6.2 漏采《需求书》§5.1 的**功耗（≤8%/h）**与**峰值内存（≤400MB）** | ✅ 采纳 | §6.2 增「功耗」行（`dumpsys batterystats` 复位 + NFR 标准条件）与 Peak PSS 采样要求 |
| **MUST-3** | 缺 native 层工具，建议引 ASan/HWASan | ⚠️ **部分采纳（技术前提纠正）** | 本仓**无自研 native 源码**（实查无 `*.cpp`/`CMakeLists.txt`），SherpaONNX 为**预编译 AAR**（`sherpa-onnx-1.13.8.aar`）⇒ ASan/HWASan **无法插桩第三方 .so**，直接引入不可行。改为官方成熟替代：tombstone + `ndk-stack` + Perfetto native heap profile + Native Heap 曲线（§二新增行）；同时在《实现报告》回写说明「《需求书》§5.2 提及的 ASan 在本项目形态下不适用及替代手段」，避免文档背离现实 |
| **MUST-4** | 九条红线中 5/6/7/9 未落到清查矩阵 | ⚠️ **部分采纳** | 新增「九条红线专项」行**逐条**给出检查点与证据要求（红线 5/7 确为原稿遗漏，已补）。但**红线 6（微淡化边界）与红线 9（SAF 强制 SafeExtractor）原稿已在矩阵内**——分见「engine 合成链路」行"微淡化边界"与「engine 解压与导入」行"SAF 强制度"，非遗漏；本次一并收纳进专项行以消除歧义 |
| **SHOULD-1** | 审计仓专属 CI 可能污染主仓 | ✅ 采纳 | §3.3 新增第 ⑤ 步「改动白名单审查」（禁 `.github/workflows` 审计 job、lint 配置、secrets、代理配置、依赖变更） |
| **SHOULD-2** | 弱网/DNS 黑洞下在线 TTFT 退化与回落未验 | ✅ 采纳 | §四 新增「异常注入与弱网」行（DNS 黑洞 / 极高延迟 / 限速丢包 → 静默回落且不无声） |
| **MAY-1** | 记录热降频状态，防 A/B 被温度误导 | ✅ 采纳 | §6.2 新增「环境量（防误判）」行 |
| 复核方"无法确认"项 | 真机供给、OEM 冻结行为、MiMo 限流扰动 | ✅ 登记 | 真机/OEM → DQ-P7-2 与 §十 R8；MiMo 扰动 → §十 R2（每次记录时间与出口，政策变更即作废重测） |
| 补充（复核未提，实施线自查） | 工具链兼容性风险 | ✅ 采纳 | §2.1「工具链兼容性实查」：detekt 稳定线不覆盖 Gradle 9.x/AGP 9.x、2.0.0 仍 alpha；OWASP 13.0.0、JMH 插件 0.7.3 对 Gradle 9 无官方声明 ⇒ 全部准备 CLI 降级路径，P7-A 首步实测回填 |

---

## 十四、甲方批复登记（2026-09-16）

| 编号 | 事项 | 甲方决定 | 实施线备注 |
|---|---|---|---|
| **DQ-P7-1** | 审计仓库形态 | **甲方 2026-09-16 二次澄清 → 定为「公开主仓 + 私密测试仓」双仓形态**（详见 §3.2 方案 A'） | 已写入 §3.2；私密仓命名 `ebook2tts-local-p7`（private），主仓 `main` 工作期零改动 |
| **DQ-P7-2** | 性能口径与真机 | **先按模拟器做相对 A/B 与基线，报告如实标注限制** | 报告固定标注「模拟器相对值」；《需求书》§5.1 绝对指标本轮不作达标判定 |
| **DQ-P7-3** | 审计工具依赖 | **允许**（detekt / JaCoCo / JMH / kotest-property / OWASP dependency-check / LeakCanary，全部准备 CLI 降级路径） | 仅审计分支与本地；主仓运行时依赖零新增；工具接入前先过 §2.1 兼容性实测 |
| **批复** | 计划表 v0.2 | **批准，按 P7-A（工具链 + 基线）开工** | P7-A 首动作＝§2.1 工具链逐项实测回填 |
| DQ-P7-4 | release 正式包 | 本轮未决 | 待甲方提供签名 keystore 后再议；实施线不代持密钥 |
| DQ-P7-5 / DQ-P7-6 | 修复范围红线 / 优先级 | 未单独裁决 | 默认按计划建议执行（最小改动＋文档同步；A/B 取证 → C 分级修复 → D 并行基线） |

---

## 十五、变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.1 | 2026-09-16 | 立稿：目标/范围、方法总则、仓库与分支流程、清查矩阵、台账规范、性能协议、批次划分、纪律、交付、风险、待裁决项 |
| **v0.2** | 2026-09-16 | **并入 agy v0.1 复核处置**（§十二）：采纳 MUST-1/2、SHOULD-1/2、MAY-1；**部分采纳并纠正** MUST-3（ASan 对预编译 AAR 不可插桩，改用 ndk-stack/native heap profile）与 MUST-4（红线 6/9 原稿已在矩阵，红线 5/7 补入专项行）；新增 §2.1 工具链兼容性实查、§四 红线专项/异常注入与弱网/OEM 策略三行、§6.2 功耗与 Peak PSS 与温度量、§3.3 改动白名单闸门、§十 R8~R10 |
| **v0.2.1** | 2026-09-16 | **登记甲方批复**（新增 §十四）：DQ-P7-1 = C（主仓分支 `p7/audit`，并登记与原话偏差）、DQ-P7-2 = 模拟器相对口径、DQ-P7-3 = 允许审计工具、计划表批准开工 P7-A |
