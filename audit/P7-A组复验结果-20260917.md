# A 组真机复验结果（2026-09-17，Mi MIX 2S / SDM845 / EvolutionX Android 14）

设备：`192.168.1.162:42941`（无线调试端口已随机化；序列号 ae7831b9 校验通过）
包：debug，构建于 20:34（含 P7 第二批全部改动；dex 内 `RELEASE|` 字符串 ×4 佐证代码在包内）

## A1 通知权限 —— ✅ 通过（实测）
- `pm revoke` 回退后启动 App，**uiautomator 抓到系统弹窗**：
  `text="要允许“书声本地”向您发送通知吗？"` + 按钮 `resource-id=com.android.permissioncontroller:id/permission_allow_button` text="允许" bounds=[137,1134][943,1288]
- 自动点击中心点 (540,1211) 后复检：
  `android.permission.POST_NOTIFICATIONS: granted=true, flags=[USER_SET|USER_SENSITIVE_WHEN_GRANTED|...]`
- **结论**：IM-544 运行时申请链路**真机验证通过**（弹窗出现 → 授权落库）。
- 注：首轮脚本报 ❌ 是**我的检测写法错**（`dumpsys | grep -m1` 取到请求清单行 + SIGPIPE broken pipe），非产品问题。

## A2 线程=2 新默认 —— ✅ 通过（实测，创该机最好成绩）
| 配置 | synth ms | 备注 |
|---|---|---|
| 7 线程（旧默认） | 107,994 | E3 实测 |
| 4 线程（上一版自动） | 83,611 | E3 实测 |
| **2 线程（本次默认）** | **66,455** | `threads=2` 实测确认；`total_ms=72,232`；同文本 bytes=273322 |
- 改善：比 4 线程 **−20.5%**、比 7 线程 **−38.5%**。
- 对照同轮：20:42:35 A2 的 66,455 ms 与 20:45 缓存测试的 66,995 ms 一致（同一档位稳定）。

## 缓存（B4）—— ✅✅ 实测超额通过（RQ-518 验收）
```
第 1 次：CACHE|miss|model=kokoro-int8|spk=4|chars=24 → PREVIEW|synth ms=66995  bytes=272862
第 2 次：CACHE|hit |model=kokoro-int8|spk=4      → PREVIEW|synth ms=4      bytes=272862
```
- 命中耗时 **4 ms vs 66,995 ms**（≈ 0.006%，RQ-518 要求 ≤5%）；**输出字节数完全一致**（272862）✓；
  播放阶段 `segments_done total_ms=5650` 与首次一致 ⇒ **外部行为未变**（红线 3/6）。

## A3 内存压力释放 —— ⚠️ 未取得实测证据（**不宣称通过**）
- 手段：`am send-trim-memory <pid> {RUNNING_LOW,RUNNING_CRITICAL,COMPLETE}` 打到 `:tts_service`（前景/后台各试一轮），
  **未观察到任何 `RELEASE|` 日志**；另 `UI_HIDDEN` 不是 `am` 认的级别名（`Error: Unknown level option`）。
- 已排除"代码没进包"：源码与 dex 均含 `onTrimMemory`/`releaseIfIdle`/`previewActive` 置位与清除。
- **判定**：属于**验证手段局限**（本 ROM/Android 14 上 `am send-trim-memory` 未见送达），**不能据此认定功能无效**，也**不得宣称已验证**。
- 下一步可取路径（未做）：① 用 `stress-ng`/大内存占用在设备上制造真实压力，等系统自发 trim（最接近现场）；
  ② 走 `onDestroy` 路径（stopService）验证释放与再预热代价；③ 前后 `dumpsys meminfo` PSS 对比佐证。
