# P7 待办清单（2026-09-16 版，含状态与阻塞）

## A. 立刻可做（无需你介入）
| # | 事项 | 状态 |
|---|---|---|
| A1 | **App 端到端在线测试**（合规路径：密钥→设备 `/data/local/tmp/k.txt`→设置页 `input text` 填入→试听；测首音/整句耗时） | 进行中 |
| A2 | **debug-only 线程数覆盖**（已批准）→ 真机实测 `threads=7/4/2` 合成耗时 | 待做 |
| A3 | **文档三件套落地**（已批准）：需求书 v1.3 / 架构书 v0.6 / 实现报告 v1.4（RQ-510~512、ADR-015、IM-523~530） | 待做 |
| A4 | **设备设置口径 + ABI 表述**写入 README/AGENTS（真机不还原、模拟器已回退；ABI 收敛随 RQ-512） | 待做 |
| A5 | **BUG-P7-002 整改**（密钥 Keystore 加密存储 + `allowBackup=false` + `dataExtractionRules`）纳入 A3 文档批次 | 待做 |
| A6 | **BUG-P7-017 修法**：据 A2 数据在 调线程 / 试听换小模型 / 同文本+音色缓存 三者中定案并实施 | 待 A2 |
| A7 | **BUG-P7-016 结案复测**：模拟器 + 真机双环境同一实验，给修复前后对照 | 待 A6 |
| A8 | 剩余台账项实施：P7-003 通知权限、005 死代码、006 detekt 235 项、007 依赖陈旧、009 空间预检、010 无效权限、012 死资源 | 待做 |
| A9 | 脚本尾巴：profiler `thermal_zone` 单位混用；`adb-guard.sh` 补网络目标（host:port）分支 | 待做 |
| A10 | **agy 第 5 轮复核**（实施结果）→ 出《P7-C 实施与复测报告》 | 待 A6/A7 |
| A11 | **合并闸门**：私密仓全绿 → 推主仓 `p7/landing` → 开 PR → 你点合并 | 待 A10 |

## B. 等你
| # | 事项 | 阻塞什么 |
|---|---|---|
| B1 | **release keystore**（或授权我用**临时 debug keystore 签一个"仅验证"的 release 变体**，结论标注待正式密钥复验） | P7-D 整批：包体 / R8 混淆 keep 规则 / lintRelease / 依赖升级验证 |
| B2 | **1 小时独占真机时段**（你说下班后） | ARM 功耗项 |
| B3 | 确认 A5 的整改范围是否就按我列的三条（Keystore 加密 + 关备份 + 数据提取规则） | A3 文档批次内容 |

## C. 已完成（本轮）
- 单例修正 + R1/R3/R4/R5/R6b/R7 实施；双环境验证（加载 1 次、`borrowed=true`、模拟器全流程 `done`）
- BUG-P7-016/017 定位（原生推理单线程 + 大核未升频 ⇒ 慢而非卡）；在线 vs 本地对照（1.60s vs >37s）
- 凭据事故处置（泄露密钥 shred、教训入 `security-lessons`、事故登记入报告）
- 真机息屏钉死（调试机不还原）；模拟器设置已回退；`adb-guard.sh` USB 自愈
- 批准项：回写申请单（RQ-510~512/ADR-015/IM-523~530）、debug-only 线程数覆盖、设备设置口径

---

## D. 合并授权与推主仓口径（甲方 2026-09-16 明确）
- **甲方指令**："都干完了就推到主仓库吧" ⇒ **授权**：P7 批次全部完成后**推送主仓库**，不再逐项请示。
- **执行口径**（沿用已定方案 A'，不擅自改为直推 main）：
  1. 私密仓 `p7/audit` 全绿（单测/构建/双环境实测）；
  2. agy 复核实施结果（第 5 轮）无 MUST 未闭环；
  3. 推 `p7/landing` 到**公开主仓** → 开 PR → **甲方点合并**（主仓 `main` 至今零改动：`3b257e2`）。
- 若甲方意欲**直推 main**（跳过 PR），需明确一句；否则按上面 PR 口径执行。

## E. 推主仓前的剩余清单（未完成，逐项对齐）
| # | 事项 | 状态 |
|---|---|---|
| E1 | RQ-513~516 / DQ-12~13 落到**架构书**（模式选择、性能分级与警告、判定实现）与**实现报告**（IM 续排） | ⬜ 未做 |
| E2 | P7-D 阻塞解除 / 签名策略 / 备份关闭 回写三件套 | ⬜ 未做 |
| E3 | debug-only 线程数覆盖 → 真机实测 `threads=7/4/2` | ⬜ 未做 |
| E4 | BUG-P7-017 修法定案并实施（调线程/模型档位/合成缓存） | ⬜ 未做 |
| E5 | BUG-P7-016 双环境结案复测（模拟器+真机，修前修后对照） | ⬜ 未做 |
| E6 | 低配判定验证（`Build.SOC_MODEL` 实际值 + 玄戒 O1/O3 型号串 + 微基准阈值标定） | ⬜ 未做 |
| E7 | 剩余台账项：P7-003 通知权限 / 005 死代码 / 006 detekt / 007 依赖 / 009 空间预检 / 012 死资源 | ⬜ 未做 |
| E8 | agy 第 5 轮复核 → 《P7-C 实施与复测报告》 | ⬜ 未做 |
| E9 | **推 `p7/landing` + 开 PR** | ⬜ 待 E1~E8 |

## F. 状态回填（2026-09-16 21:5x）
| 项 | 更新 |
|---|---|
| E1 | ✅ 完成（架构书 v0.7 / 实现报告 v1.5，提交 32ed28c） |
| E2 | ✅ 完成（同批；含 P7-D 阻塞解除、签名策略、备份关闭） |
| E3 | ✅ 完成（线程数 A/B 实测，提交 d4e404a） |
| E4 | ⚠️ **修法修正**（发现与 IM-522 冲突 ⇒ 改为拓扑感知），文档先行后实施 |
| E5~E9 | ⬜ 未开始 |
