# AGENTS.md — ebook2tts-local

> 本仓库的**项目级约定唯一权威**（Agent / 协作者开工前必读）。
> 版本：v1.0 ｜ 2026-09-14

---

## 1. 文档权威（最高优先级）

- 重构三件套为唯一权威：**《需求书》**（需求 `RQ-`）→ **《架构书》**（架构 `AR-` / 决策 `ADR-`）→ **《实现报告》**（实现 `IM-` / 验收 `TC-`）。
- **改行为先改文档**（依变更记录升版），再动代码；**实现与文档不一致＝缺陷**（双向：代码背离文档、文档背离现实，都要回写）。
- 编号全链路可追溯：`RQ-` → `AR-`/`ADR-` → `IM-`；**编号不复用、不重排**，废弃条目标注 `deprecated`。

---

## 2. 协作与角色

- **实施**：Hermes（本机实施）。
- **独立复核**：agy（只读、可驳回；每批次复核报告必须含「9 条防呆红线逐条核验表」）。
- **验收拍板**：Kermond（甲方）。
- **裁决优先级**：官方源码 / 官方文档 ＞ 本机可复现实测 ＞ 推理；僵持升级甲方终审。
- **批次流程**：批次计划 → 实施＋自测 → 证据归档 → agy 独立复核 → 处置 → 状态回填（commit 短 SHA）。

---

## 3. 工程规范

- 模块划分：`:core`（纯 JVM 逻辑，可单测）/ `:engine`（Android 库：TTS 服务、下载、模型管理）/ `:app`（**Flutter** 配套 App）。
- 引擎服务运行于独立进程 `:tts_service`；**主进程零推理**（试听合成＋播放同样收敛进引擎进程）。
- `minSdk 27` / `targetSdk 34`；正式包 `arm64-v8a + armeabi-v7a`；debug 保留 `x86_64`（模拟器链路）。
- 提交前须全绿：`:core:test`、`:engine:testDebugUnitTest`、`flutter test`、CI。
- **禁止提交**：密钥/凭据、内网地址与主机名、本机绝对路径、构建产物、模型文件（大文件走下载流程）。

---

## 4. 九条防呆红线（代码审查必查）

见《实现报告》§1.3。摘要：

1. 绝不为内部原因（重载/刷新/回收）中止在途朗读请求；
2. 绝不在已有音频时调用 `error()` 强停；
3. 绝不"假成功"（非空文本零音频必须 `error` 可见）；
4. 主进程零推理；`:tts_service` 进程零 Flutter；
5. native 释放只允许在守卫锁内、无任何持有者时；
6. 微淡化仅限请求首块头部 / 末块尾部，严禁下沉进流式分段路径；
7. 合成线程严禁 IO、去抖或锁长等待（配置与映射预构建）；
8. 存量模型回填 `.completed` 哨兵前**必须通过清单校验**；
9. SAF 离线导入**必须强制经 SafeExtractor**，严禁另写解压循环。

---

## 5. 交付与署名

- 署名 **Kermond**；LICENSE **MIT**（kermond369-nsis）。
- 构建产物经 `gh release` 发布（tag 与 `versionName` 对齐）；**文档交付不经 GitHub**。
- 界面上不出现工程黑话与英文残留（用户可感知文案一律中文）。

---

## 6. 子代理（subagent / 委派任务）纪律

> 起因：2026-09-15 两个委派子代理**两次**擅自在系统技能库创建技能（`android-emulator-headless`），
> 违反「技能只在用户明确要求时创建」的既定纪律，由实施线两次删除。甲方当日要求写入本文件。

1. **禁止创建/修改技能**：子代理**一律不得**调用技能管理能力（创建、改写、删除技能）。
   技能只由**用户在明确要求**时创建；违反即视为越权，所建技能必须删除并在批次记录中留痕。
2. **禁止越界写盘**：子代理只能改动**任务书明确指定的目录**；对主仓库的实验性改动必须在其**副本**中进行，
   主仓库保持只读（含"只 chmod 一个文件"这类小动作也不允许）。
3. **禁止自行扩权**：不得新增依赖、不得改 CI、不得 push/建 PR、不得创建计划任务（cron），
   除非任务书逐条明确授权。
4. **必须交回可核查证据**：结论须附命令原文或日志路径；**"我没跑/没验证"要明写**，
   禁止用推断冒充实测结果。
