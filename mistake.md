# mistake.md · 错题本

> 本文件记录**本项目自初始（在线语音引擎接入）阶段、经项目交接、至今**由实施线（AI 助手）犯下的错误与修正，
> 并按「现象 → 根因（抽象为逻辑）→ 规则」整理，作为日后的防呆清单。
>
> **本文件不含任何敏感信息**：不出现密钥/凭据、设备序列号、内网地址与主机名、本机绝对路径、账号信息。
> 涉及具体环境的部分已抽象为通用描述。

---

## A. 事实与结论类

### A1 凭空断言平台行为
- **现象**：曾把"某系统设置在长时间息屏后会自动关闭"当作事实写进结论与说明。
- **根因（逻辑）**：用**听起来合理的因果关系**替代查证；平台行为涉及版本差异，"经验记忆"不可靠。
- **规则**：涉及系统/平台/版本行为的任何断言，必须查**官方文档或源码**并留出处；查不到就标注"**未验证**"，不得作为结论。

### A2 凭印象给型号/数字
- **现象**：曾报出一个**并不存在**的硬件型号；也曾用"约 XX 秒"的口头估计替代实测。
- **根因（逻辑）**：把"记忆中的近似"当成"已核实的值"。
- **规则**：型号、版本、数字一律**现场查证/实测**并留证据（命令输出、日志行）；查不到就说不知道。

### A3 用非产品环境的结论代表产品
- **现象**：拿**模拟器**（同构核）的实测结论去论证**真机**（异构核）的最优参数，被甲方纠正。
- **根因（逻辑）**：混淆"可用工具"与"目标环境"；把低成本环境的结果当作产品事实。
- **规则**：产品只在**目标真机**上定论；模拟器/仿真环境的结果必须标注「**机制级**」，不得作为产品结论。

### A4 数据不足即给"最优值"
- **现象**：用单机单场景数据给出普适"最优参数"。
- **根因（逻辑）**：结论的**适用域**未界定，等于把特例当通例。
- **规则**：结论必须写明适用域（哪类硬件/哪种负载）；跨域差异大的，给出分档策略而非单点最优。

### A5 未读完材料就处置
- **现象**：收到独立评审意见后，只读了前半段就开始逐条处置并合并。
- **根因（逻辑）**：把"已经足够形成观点"误当作"已经读完"。
- **规则**：评审/反馈**读完整**再行动；处置时逐条留痕（采纳/驳回＋理由）。

---

### A6 未显式设置提交身份 ⇒ 署名到他人账号
- **现象**：在未设置 git 身份的环境里提交，git 回落到**系统用户名**（恰好等于某人的电脑账户名），邮箱又用了 noreply 格式 ⇒ 提交被**署名到 GitHub 上一位无关的第三方账号**；已推送后才由甲方发现。
- **根因（逻辑）**：把"能提交"当作"署名正确"；**默认值**在网络平台上有真实后果（归属、贡献统计、法律意义上的作者），却没有被当成必检项。
- **规则**：提交**必须显式指定身份**（姓名 + 邮箱），代理/工具同样；**禁用** `<他人用户名>@users.noreply.github.com` 这类会把提交挂到他人账号的邮箱；交付前用一条命令核对全部作者身份；一旦发现错署名，**立刻改写历史并强推**，同时核对**所有仓库**（不只当前这一个）。

## B. 观测与验证类

### B1 假仪表（观测值不是生效值）
- **现象**：日志打印的是**配置值**而非**实际生效值**，据此得出"参数未生效"的错误结论；另一个案例里把线程的**名字**当成了**实例数**。
- **根因（逻辑）**：观测点选在了"输入侧"而非"生效侧"；对可疑数字缺少交叉验证。
- **规则**：观测必须落在**生效路径**（实际被使用的值）；关键数字至少用**两条独立证据**交叉验证（内存快照、加载次数、实际耗时）。

### B2 测试前提未自检
- **现象**：为验证"本地路径的释放行为"，却在**在线模式**下测试（本地后端根本没加载），得到"无变化"的假结论。
- **根因（逻辑）**：只设计了"操作"，没有设计"**前置断言**"。
- **规则**：测试脚本必须有前置断言（如"后端已加载""模式=本地"）；**前提不成立即判定该轮无效**，不得据无效轮下结论。

### B3 跨版本系统限制未先查
- **现象**：用**隐式广播**触发接收器，在新版本系统上根本不会被投递，导致"钩子没跑"却被当成"功能有问题"。
- **根因（逻辑）**：沿用旧版本的用法，未核对新版本的行为变更。
- **规则**：涉及系统 API/机制时，先核对**当前目标版本**的行为差异；测试通路先做**最小可用性验证**（先确认"能触发"，再验证"业务正确"）。

### B4 脚本解析类低级错误
- **现象**：shell 变量解析/算术展开写错，导致脚本"跑完却什么都没做"（还误以为是被测对象的问题）。
- **根因（逻辑）**：脚本没有自证输出；失败静默。
- **规则**：关键解析/坐标/参数在脚本内**先打印自证**；关键步骤失败要**能看见**（显式断言与中止）。

---

## C. 工程与流程类

### C1 构建配置期抛异常把 CI 弄红
- **现象**：为"缺凭据即失败"的护栏写在构建脚本的**配置期**，导致主线 CI（不含凭据）在所有任务上失败。
- **根因（逻辑）**：没区分**配置期**与**任务期**执行时机；护栏的生效面大于预期。
- **规则**：构建期校验放在**任务期**（任务前置检查）；改构建脚本必须**真跑一次构建**（静态检查看不见脚本编译/时机问题）。

### C2 修改正在运行的脚本
- **现象**：边运行边改脚本，导致解释器读到半截文件而中断（报"未闭合引号"式错误）。
- **根因（逻辑）**：脚本是**流式读取执行**的，文件即"正在执行的代码"。
- **规则**：**运行中的脚本禁止编辑**；需修改就先停、改、再跑（或另存新名跑）。
  （**2026-09-19 复犯一次** ⇒ 已把"改脚本前先停进程"纳入操作习惯：改前先 `pkill` 停旧实例。）

### C3 测试钩子流入交付物
- **现象**：为实测临时加入的覆盖/触发钩子，一度有进入正式产物的风险。
- **根因（逻辑）**：临时手段与交付物放在同一变体里，缺少"变体边界"。
- **规则**：测试钩子只允许存在于 **debug 变体**（或独立分支），并纳入"交付前检查清单"。

### C4 为满足静态检查而收窄错误处理
- **现象**：为消除静态规则告警，把边界处的"宽捕获"改成窄捕获；这会让底层错误**逃逸成崩溃**。
- **根因（逻辑）**：把"规则的字面满足"置于"运行时安全"之上。
- **规则**：静态规则服务于安全，**边界守卫（native/回调/线程）除外并留理由**；收窄前必须能证明异常类型覆盖完整。

### C5 未核实就归因
- **现象**：把偶发断连归因于某个"硬件特性"，后经核验并不成立。
- **根因（逻辑）**：先有假设，再找证据；缺少反证步骤。
- **规则**：先取证再归因；**归因必须能被反证**（给出能推翻它的观测）。

---

## D. 协作与交付类

### C6 版本号"只改文末、漏改头部"
- **现象**：跨批次追加文档章节时只在文末变更记录里加版本行，**头部"版本："行未同步**；对外说明（README）里也残留旧版本号。由甲方一眼看出。
- **根因（逻辑）**：把"追加内容"当成"升版完成"；版本信息有**多处副本**却只改了一处，且缺少一致性自检。
- **规则**：升版必须**全文一致**（头部版本行、变更记录、对外对照表、包版本号四处同改）；改完做一次**全文版本号自检**（检索旧号残留），并纳入交付前检查清单。

### C7 未校验上下文就注入交互（误触系统界面）
- **现象**：自动化脚本按固定坐标点击，未先确认**前台是哪个应用**；一次误落在系统页面上，**把设备的调试开关关掉**，随后一连串失败还被误判为"设备/网络问题"。
- **根因（逻辑）**：把"坐标"当成"语义"，忽略了交互的**上下文前提**；失败后又先怀疑环境而非自身动作。
- **规则**：任何点击/输入前**必须校验前台上下文**（如当前焦点应用）；不匹配就**先复位再操作**，连续失败即**中止**而不是盲点。该前置必须写进工具函数，不依赖人记。

### C8 按名字杀进程导致自匹配
- **现象**：用模式匹配杀后台进程时，**把执行命令的自身 shell 一起杀掉**（命令文本里包含同样的模式），现场表现为"命令被莫名终止"。
- **根因（逻辑）**：模式匹配的作用域包含了执行者自身。
- **规则**：按**括号模式**（如 `xxx[e]`）或**按 PID** 杀进程；不确定就先列出来确认再杀。

### D1 把"列清单"当交付
- **现象**：把待办清单交出去就停下，未推进到可验证的完成态。
- **根因（逻辑）**：把"计划"与"交付"混同；把"一批做完"当收工点。
- **规则**：交付＝**可验证的完成态**（实测证据 + 可复现入口）；未完成不得以清单收尾。

### D2 越权写操作
- **现象**：对远端仓库的写操作（推送/发布）缺少显式授权确认。
- **根因（逻辑）**：把"技术上可行"当作"流程上允许"。
- **规则**：写操作先获授权；文档交付走约定的离线渠道，不擅自上远端。

### D3 泄密面：原始输出回显
- **现象**：把未经过滤的原始输出（可能含凭据）直接回显，导致凭据进入会话记录。
- **根因（逻辑）**：脱敏规则按**已知前缀**匹配，未知前缀漏网；未做"默认不输出"。
- **规则**：采用**前缀无关**的白名单式清洗；原始输出默认不回显；凭据一旦进入会话即视为**已泄露**，立即轮换。

### D4 后台进程失控
- **现象**：验证用的后台进程未回收，长时间占用算力未被发现。
- **根因（逻辑）**：启动与回收不对称，缺少"任务结束即回收"的纪律。
- **规则**：验证进程**用完即 kill**；长任务命名可追溯、有超时；定期核对存活列表。

### D5 素材凭来源猜内容
- **现象**：按文件名/来源推断图片内容并直接采用，实际内容不符。
- **根因（逻辑）**：把"来源可信"当作"内容正确"。
- **规则**：视觉素材**先渲染自查**（人工/视觉核对）再进产品。

---

## E. 记录格式（新增条目请沿用）

```
### Xn 一句话标题
- 现象：
- 根因（逻辑）：
- 规则：
```

> 说明：本错题本只记**通用教训**，不含具体环境信息、密钥、设备标识、网络位置与个人路径。
